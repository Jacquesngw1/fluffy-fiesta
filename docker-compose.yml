version: "3.8"

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  # Uncomment if you want a PostgreSQL database for audit history
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: geo_audit
  #     POSTGRES_PASSWORD: geo_audit_pass
  #     POSTGRES_DB: geo_audit_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

volumes:
  redis_data:
  # postgres_data:
